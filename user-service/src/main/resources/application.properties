# Server port
server.port=8081

# Spring App Name
spring.application.name=user-service

# PostgreSQL Datasource Configuration
# Note: Docker exposes PostgreSQL on host port 3306 (mapped to container's 5432)
spring.datasource.url=jdbc:postgresql://localhost:3306/ride_sharing_db
spring.datasource.username=admin
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
# Base64 encoded secret key (minimum 256 bits for HS256)
jwt.secret=dGhpc2lzYXZlcnlsb25nc2VjcmV0a2V5Zm9yaHMyNTZhbGdvcml0aG1taW5pbXVtMjU2Yml0cw==
# Token expiration: 24 hours in milliseconds
jwt.expiration=86400000
